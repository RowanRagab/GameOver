import { AboutGamePipe } from './about-game.pipe';

describe('AboutGamePipe', () => {
  it('create an instance', () => {
    const pipe = new AboutGamePipe();
    expect(pipe).toBeTruthy();
  });
});
